const express = require('express');
const router = express.Router();



/**
 * App Routes 
*/
//Super router, make sure that we have our functions from our module we create#
// Ex: router.get('/user', <- so when );

//Export for app.js
module.exports = router;